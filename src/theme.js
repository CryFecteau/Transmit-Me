import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#1760a5',
            light: '#skyBlue',
        },
        secondary: {
            main: '#15c630',
        },
        otherColors: {
            main: '#999',
        }
    }
})